# Agent Instructions

## Command Execution Rules

For every configuration file located in the `/configs` directory, execute the `epibench create` command using its path:

```bash
epibench create --config-path "<path to config>"